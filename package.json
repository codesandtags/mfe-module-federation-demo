{
  "name": "mfe-module-federation-demo",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "serve": "wsrun --parallel serve",
    "start": "concurrently \"wsrun --parallel start\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^5.1.0",
    "lerna": "^8.1.3",
    "wsrun": "^5.2.0"
  },
  "dependencies": {}
}
